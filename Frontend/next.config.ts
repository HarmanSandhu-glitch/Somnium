// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "assets.aceternity.com",
      "source.unsplash.com",
      "images.unsplash.com",
    ],
  },
};

module.exports = nextConfig;
